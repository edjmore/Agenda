package com.ifthenelse.ejmoore2.agenda;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by edward on 1/23/17.
 */
public class DatetimeUtilsTest {

    /* private final long START_TIME =
            DatetimeUtils.ONE_WEEK * 2 + DatetimeUtils.ONE_DAY * 3 +
                    DatetimeUtils.ONE_HOUR * 11 + DatetimeUtils.ONE_MINUTE * 45;
    private final long END_TIME = START_TIME +
            DatetimeUtils.ONE_DAY + DatetimeUtils.ONE_MINUTE * 33;

    @Test
    public void getRelativeTimeStringOccurs() throws Exception {
        String result = DatetimeUtils.getRelativeTimeString(0, START_TIME, START_TIME);
        assertEquals("occurs in 2 weeks 3 days 11 hours 45 minutes", result);
    }

    @Test
    public void getRelativeTimeStringBegins() throws Exception {
        String result = DatetimeUtils.getRelativeTimeString(0, START_TIME, END_TIME);
        assertEquals("begins in 2 weeks 3 days 11 hours 45 minutes", result);
    }

    @Test
    public void getRelativeTimeStringEnds() throws Exception {
        String result = DatetimeUtils.getRelativeTimeString(START_TIME, START_TIME, END_TIME);
        assertEquals("ends in 1 day 33 minutes", result);
    } */
}